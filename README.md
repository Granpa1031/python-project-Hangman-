# python-project-Hangman-
This project demonstrates how to transform purely logical Python code into an interactive application with a "product feel". Through the encapsulation of ipywidgets and HTML/SVG, we achieved an experience comparable to web games in the Jupyter environment and resolved the conflict between network requests and interface rendering.
